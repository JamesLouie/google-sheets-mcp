# OAuth 2.0 Configuration for Google Sheets MCP Server
# Copy this file to .env and update the paths

# OAuth 2.0 Credentials (required)
CREDENTIALS_PATH=./credentials.json
TOKEN_PATH=./token.json

# Optional: Default folder for spreadsheet operations
# DRIVE_FOLDER_ID=your_google_drive_folder_id_here

# Instructions:
# 1. Download OAuth credentials from Google Cloud Console
# 2. Save as 'credentials.json' in project root
# 3. Copy this file to '.env'
# 4. Run 'npm start' - OAuth will be initiated automatically if needed
# 5. Complete browser authorization when prompted 